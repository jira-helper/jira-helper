---
name: task-template
description: Стандартный шаблон задачи и EPIC для jira-helper. Используй при создании новых задач и эпиков; все артефакты одной фичи — в одной подпапке .agents/tasks/<FEATURE-SLUG>/.
---

# Шаблон задачи jira-helper

## Папка фичи (feature bundle)

Всё, что относится к **одному** запросу/эпику, лежит в **одной** директории — её можно удалить целиком.

### Старт из чата (без ручной подготовки)

Пользователь **не обязан** заранее создавать папку или `request.md`. Достаточно описать фичу в чате (лучше с агентом **feature-orchestrator**).

**Агент в первом же ответе должен:**

1. Выбрать `<FEATURE-SLUG>` (kebab-case; при коллизии имён папок — уточнить у пользователя или предложить суффикс).
2. Создать `.agents/tasks/<FEATURE-SLUG>/` и файл **`request.md`** с датой, заголовком и текстом запроса из чата.
3. Дальше вести EPIC/TASK/requirements **только** в этой папке.

Если пользователь **уже** указал существующую папку (например «продолжи в `.agents/tasks/foo-bar/`») — не дублируй slug, используй её и дополни `request.md` новым сообщением или секцией «Дополнение».

**Базовый путь:**

```
.agents/tasks/<FEATURE-SLUG>/
```

**`<FEATURE-SLUG>`**: латиница, kebab-case, отражает тему (например `field-limits-modernization`, `shared-di-refactoring`). Не используй пробелы.

**Рекомендуемые файлы внутри папки:**

| Файл | Назначение |
|------|------------|
| `request.md` | Исходный запрос пользователя (копия или ссылка на контекст) |
| `requirements.md` | Согласованные требования и ограничения |
| `EPIC-{N}-{short-name}.md` | EPIC с таблицей задач |
| `target-design.md` или `target-design-{name}.md` | Target Design (по скиллу solution-design) |
| `*.feature` | BDD-сценарии — **если** хранишь их здесь; иначе ссылка в EPIC на путь в `src/` |
| `TASK-{N}-{short}.md` | Атомарные задачи |
| `delivery.md` | Итог релиза, changelog, ссылки (опционально) |

**Legacy:** файлы прямо в `.agents/tasks/*.md` (без подпапки) — старые эпики; **новые** артефакты создавай только внутри `.agents/tasks/<FEATURE-SLUG>/`.

**Одноразовая задача без полноценного EPIC:** подпапка `.agents/tasks/_standalone/<topic-slug>/` с `TASK-{N}-*.md` (и при необходимости `request.md`); **Parent**: Standalone.

---

## Стандарты

### Статусы

Используй только эти статусы (одинаковые в файле задачи и в EPIC таблице):

| Статус | Описание |
|--------|----------|
| `TODO` | Не начата |
| `IN_PROGRESS` | В работе |
| `VERIFICATION` | Агент завершил, ожидает ручной проверки пользователем |
| `DONE` | Завершена и проверена пользователем |

### Именование файлов

`TASK-{N}-{короткое-описание}.md`

Примеры:
- `TASK-20-add-tests-coverage.md`
- `TASK-6-create-settings-button-view.md`

### Нумерация

Номера **TASK** и **EPIC** глобальные по репозиторию (как раньше), независимо от папки фичи.

**Перед созданием задачи** определи следующий номер (все подпапки + legacy в корне):

```bash
find .agents/tasks -name 'TASK-*.md' -type f 2>/dev/null | sed 's/.*TASK-\([0-9]*\).*/\1/' | sort -n | tail -1
```

Используй `max + 1`.

**Перед созданием EPIC** — следующий номер EPIC:

```bash
find .agents/tasks -name 'EPIC-*.md' -type f 2>/dev/null | sed 's/.*EPIC-\([0-9]*\).*/\1/' | sort -n | tail -1
```

Используй `max + 1`.

---

## Шаблон TASK

```markdown
# TASK-{N}: {Краткое название}

**Status**: TODO
**Type**: {тип сущности или характер работы, например: types, model, view, container, stories, page-object, api, di-wiring, bdd-tests, config, refactoring, other}

**Parent**: [EPIC-{N}](./EPIC-{N}-{name}.md)

---

## Описание

{Что нужно сделать и зачем. 2-3 предложения.}

## Файлы

{Список файлов для создания/изменения}

\`\`\`
src/path/to/
├── file1.ts        # новый / изменение
└── file2.test.ts   # новый
\`\`\`

## Что сделать

{Пронумерованные шаги}

1. {Шаг 1}
2. {Шаг 2}
3. {Шаг 3}

## Критерии приёмки

- [ ] {Критерий 1}
- [ ] {Критерий 2}
- [ ] Тесты проходят: `npm test`
- [ ] Нет ошибок линтера: `npm run lint:eslint -- --fix`

## Зависимости

{Ссылки на другие задачи, файлы, референсы}

- Зависит от: [TASK-{N}](./TASK-{N}-{name}.md)
- Референс: `src/path/to/similar-implementation`

---

## Результаты

**Дата**: {YYYY-MM-DD}

**Агент**: {Coder / Architect / Manual}

**Статус**: VERIFICATION

**Что сделано**:

{Краткое описание выполненных изменений — 2-5 пунктов}

- Создан файл X с Y
- Обновлён файл Z
- Добавлены тесты

**Проблемы и решения**:

{ОБЯЗАТЕЛЬНО! Документируй ВСЕ проблемы, с которыми столкнулся, и как их решил.}

**Проблема 1: cy.stub не работает с ES modules**

Контекст: При попытке замокать функцию из ES module cy.stub возвращал ошибку.

Решение: Использовал DI токены вместо мока модулей — зарегистрировал mock через globalContainer.
```

### Standalone задачи

Если задача не входит в EPIC (миграция инструментов, одноразовый рефакторинг):

```markdown
**Parent**: Standalone
```

Standalone задача:
- Не добавляется в EPIC таблицу
- Имеет собственные критерии приёмки
- Нумеруется в общей последовательности

---

## Атомарная декомпозиция задач

Каждая TASK имеет поле **Type** — краткое описание сущности или характера работы. Помогает coder-агенту понять scope и какой skill читать.

**Типичные значения Type** (не строгий enum — используй что подходит):

- `types` — интерфейсы, типы, DI-токены
- `model` — Model/Store класс + unit test
- `view` — View-компонент (presentation) + unit test
- `container` — Container-компонент + unit test
- `stories` — Storybook stories для компонента
- `page-object` — PageObject (работа с DOM) + unit test
- `page-modification` — PageModification (entry point фичи на странице) + unit test
- `api` — API-функция, адаптер, HTTP-клиент + unit test
- `di-wiring` — регистрация токенов в DI-контейнере, entry point
- `bdd-tests` — Cypress BDD тесты
- `config` — конфигурация (manifest, webpack, package.json)
- `refactoring` — рефакторинг без изменения поведения
- `other` — всё, что не попадает в категории выше

### Главное правило

**Одна задача = один слой + один модуль.** Если задача затрагивает и Model, и View — разбей на две.

### Правила гранулярности

1. **1 TASK = 1-3 файла** (реализация + тест). Если больше — задача слишком крупная.
2. **Stories — всегда отдельная TASK.** Не совмещай создание компонента и его stories.
3. **BDD-тесты — отдельная TASK.** Cypress тесты отделены от unit tests.
4. **DI-wiring — отдельная TASK.** Регистрация в контейнере отдельно от реализации.
5. **Unit test идёт вместе с реализацией** в одной TASK (это TDD — тест пишется первым).
6. **Конфигурация — отдельная TASK.** Изменения manifest/webpack не смешивай с кодом фичи.

### Anti-patterns (как НЕ надо разбивать)

**Слишком крупная задача:**

```
TASK: Создать Cloud PageObject
- Исследовать DOM Cloud-доски
- Написать CloudBoardPagePageObject (15 методов)
- Написать unit tests
- Зарегистрировать в DI
```

**Правильная декомпозиция той же работы:**

```
TASK-1 [types]:           Определить интерфейс IBoardPagePageObject и DI-токен
TASK-2 [page-object]:     CloudBoardPagePageObject — selectors и методы для колонок/issues
TASK-3 [page-object]:     CloudBoardPagePageObject — методы для свимлейнов и фильтрации
TASK-4 [di-wiring]:       Зарегистрировать CloudBoardPagePageObject в content-cloud.ts
```

**Ещё пример — слишком крупная:**

```
TASK: Реализовать PersonLimitsForm с моделью и stories
```

**Правильная декомпозиция:**

```
TASK-1 [types]:       Типы и интерфейсы для PersonLimitsForm
TASK-2 [model]:       SettingsUIModel — state управления формой
TASK-3 [view]:        PersonLimitsForm — presentation компонент
TASK-4 [container]:   PersonLimitsFormContainer — подключение к store
TASK-5 [stories]:     Stories для PersonLimitsForm
TASK-6 [bdd-tests]:   Cypress тесты для сценария "добавление лимита"
```

---

## Типы задач по характеру работы

### Bug Fix

Обязательные поля:
- Root cause с кодом
- Ссылка на .feature сценарий (если есть)

### Feature / Refactoring

Обязательные поля:
- Целевой код (до/после или шаблон)
- Ссылки на файлы-референсы

### Тестирование

Обязательные поля:
- Таблица покрытия (Scenario → Cypress Test)
- Команды запуска тестов в критериях приёмки

---

## Шаблон EPIC

`EPIC-{N}-{короткое-описание}.md`

```markdown
# EPIC-{N}: {Название}

**Status**: TODO
**Created**: {YYYY-MM-DD}

---

## Цель

{Что решаем и зачем. Проблема → Решение.}

## Target Design

См. [target-design-{name}.md](./target-design-{name}.md) (тот же каталог, что и EPIC — папка фичи)

## Архитектура

{Mermaid-диаграмма — обзор изменений}

## Задачи

### Phase 1: {Название фазы}

| # | Task | Описание | Status |
|---|------|----------|--------|
| {N} | [TASK-{N}](./TASK-{N}-{name}.md) | {Описание} | TODO |

### Phase 2: {Название фазы}

| # | Task | Описание | Status |
|---|------|----------|--------|
| {N} | [TASK-{N}](./TASK-{N}-{name}.md) | {Описание} | TODO |

## Dependencies

{Mermaid flowchart — граф зависимостей между задачами}

**Параллельно можно выполнять:**
- {список независимых задач}

**Последовательно:**
- {цепочки зависимостей}

## Acceptance Criteria

- [ ] {Критерий 1}
- [ ] {Критерий 2}
- [ ] Все тесты проходят
- [ ] ESLint без ошибок
```

### Правила EPIC

- **Фазы** группируют задачи по логическим блокам (инфраструктура → модели → компоненты → cleanup)
- **Dependencies** — Mermaid граф показывает что можно запускать параллельно
- **Acceptance Criteria** — для EPIC в целом, не для отдельных задач
- **Target Design** — ссылка на документ из `solution-design` скилла (если есть)

---

## EPIC таблица

При создании задачи — добавь строку в EPIC таблицу:

```markdown
| {N} | [TASK-{N}](./TASK-{N}-{name}.md) | {Описание} | TODO |
```

При завершении — обнови статус в обоих местах (файл задачи + EPIC таблица).

---

## Чек-лист создания задачи

- [ ] Выбрана папка фичи `.agents/tasks/<FEATURE-SLUG>/` (или `.agents/tasks/_standalone/<topic-slug>/`)
- [ ] Номер TASK определён (`find` по `.agents/tasks`, max + 1)
- [ ] Файл создан по шаблону **внутри** этой папки
- [ ] Status = TODO
- [ ] **Type указан** (тип сущности или характер работы)
- [ ] Parent указан (EPIC или Standalone)
- [ ] Критерии приёмки с чекбоксами
- [ ] **Задача атомарна**: 1-3 файла, один слой, один модуль
- [ ] Задача добавлена в EPIC таблицу (если не Standalone)
- [ ] Зависимости указаны (если есть)

## Чек-лист создания EPIC

- [ ] Создана/использована папка `.agents/tasks/<FEATURE-SLUG>/`
- [ ] Номер EPIC определён (`find` по `.agents/tasks`, max + 1)
- [ ] Цель описана (проблема → решение)
- [ ] Архитектура — Mermaid диаграмма
- [ ] Задачи разбиты по фазам
- [ ] Dependencies — граф зависимостей
- [ ] Acceptance Criteria для EPIC в целом
- [ ] Target Design создан (если рефакторинг/новая фича)
