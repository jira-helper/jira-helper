# QA: TASK-191 — Fix — при удалении колонки из группы не стирается старая шапка

**Дата**: 2026-04-06  
**TASK**: [TASK-191](./TASK-191-fix-header-not-cleared-on-column-remove.md)  
**Вердикт**: PASS

## Автоматические проверки

| Проверка | Результат | Детали |
|----------|-----------|--------|
| ESLint | pass | `npm run lint:eslint -- --fix` завершился с кодом 0, без ошибок |
| Tests | pass | `vitest run`: 89 файлов, 864 теста, все зелёные |
| Build | pass | `npm run build:dev` завершился с кодом 0 (`✓ built in ~3.5s`) |

## Проектные требования

| Проверка | Результат | Комментарий |
|----------|-----------|-------------|
| i18n | pass | В рамках фикса не добавлены новые пользовательские строки: изменения касаются порядка вызовов DOM (`resetColumnHeaderStyles` для всех колонок до повторного стилизования). Существующий HTML бейджа в `BoardRuntimeModel` с англоязычным hint — вне diff задачи TASK-191 |
| Accessibility | pass | Интерактивных элементов в изменённой логике `apply()` не добавлялось |
| Storybook | N/A | Задача не вводит новых View-компонентов для Storybook |

## Проблемы

Нет блокирующих замечаний по результатам автоматических проверок и ручного просмотра scope.

## Резюме

Линтер, полный прогон тестов и dev-сборка прошли успешно. Реализация соответствует критериям приёмки по коду: `applyColumnHeaderStyles` сбрасывает стили заголовка для каждой колонки до применения групповых стилей; в `BoardRuntimeModel.test.ts` есть сценарий «resets every board column header before styling so removed group columns lose decoration». Ручной сценарий в Jira (группа → Save → убрать колонку → Save) в этом QA не выполнялся.
