# Request: Days in Column — подсказка про настройку Jira

**Дата**: 2026-08-04
**Slug**: `days-in-column-jira-prerequisite`
**Тип**: UI
**Has UI**: yes
**Scope**: small

## Описание запроса

Есть фича показа дней в колонке. Чтобы она работала надо еще в настройках доски включить показ дней в колонке.

Надо добавить в UI настроек показать иконку с тултипом про это и в доке про фичу это указать как требование.
Это надо сделать, а потом залить куда то html отчет со скриншотом чтобы я увидел как это выглядит. хтмл отчет на нетлифай можно кидать. Надо будет покрыть тестами, хотя бы минимальными. Можешь юзать скилл оркестратора

## Контекст из кода (на момент старта)

В `DaysInColumnSettings.tsx` уже есть `Alert` (`data-testid="days-in-column-jira-settings-required"`) с текстом `jiraSettingsRequired`, который показывается когда бейдж включён. В доке (`website/docs/.../days-in-column.md` и ru) это требование пока не выделено явно.

В соседних настройках уже есть паттерн `InfoCircleOutlined` + Tooltip (см. `AdditionalCardElementsSettings.tsx`, `IssueLinkSettings`).

## Уточнения

### Раунд 1 (2026-08-04)

- UI уже ок (существующий Alert в настройках достаточен) — **иконку/тултип не добавляем**.
- Нужно только в доке фичи явно указать: фича работает **только если** в настройках доски Jira включено «Показывать дни в колонке» / «Show days in column».
- Пройти весь workflow оркестратора; в конце обновить сайт с документацией.
