# Лимиты WIP по ячейкам доски

| | |
|---|---|
| Где настраивается | Настройки доски → Колонки → Edit WIP limits by cells |
| Где видно | Доска (детальный вид) |
| Настройки действуют | Свойство доски Jira (`wipLimitCells`) |

## Цель

Задать единый лимит незавершённой работы для выбранного блока ячеек доски (пересечений колонок и swimlane). Область выделяется контуром, на отдельных ячейках можно включить счётчики, а при превышении лимита ячейки подсвечиваются. Диапазоны можно пометить как отключённые — для визуальной группировки без активного WIP-контроля.

## Как настроить

1. Откройте **Настройки доски**.
2. Перейдите на вкладку **Колонки**.
3. Нажмите **Edit WIP limits by cells**.
4. Добавляйте, редактируйте или удаляйте диапазоны. Для каждого диапазона задайте:
   - **Имя** — название диапазона.
   - **Лимит** — числовой порог для всех задач внутри диапазона.
   - **Отключён** — пометить как визуально отключённый (штриховка, без активного поведения WIP).
   - **Типы задач** — при необходимости сузьте, какие типы задач учитываются.
   - **Ячейки** — таблица пар swimlane–колонка, определяющих диапазон. Для каждой ячейки включите или выключите индикатор-бейдж.
5. Нажмите **Save**, чтобы сохранить для всех на доске, или **Cancel**, чтобы отменить.

Сохранять могут только администраторы доски или пользователи с правами на изменение конфигурации доски.

## Как использовать

- Закрытый блок ячеек обводится **синей пунктирной рамкой**, чтобы форма диапазона была очевидна.
- На ячейках с включённым индикатором показывается бейдж **текущее / лимит** с цветом: **зелёный** (ниже), **жёлтый** (на лимите), **красный** (выше).
- При превышении лимита все ячейки диапазона получают **полупрозрачный красный** фон.
- **Отключённые** диапазоны отображаются со **штриховкой** и не проявляют активного WIP-поведения — удобно для визуальной группировки без ограничений.
- Все задачи внутри ячеек диапазона суммируются в **один общий счётчик**.

## Сценарии использования

- «Хочу ограничить пятью задачами всё, что находится в колонке In Progress и swimlane Frontend.»
- «Хочу визуально сгруппировать блок ячеек без ограничений, пометив его как отключённый.»
- «Хочу считать только баги внутри конкретного диапазона ячеек.»
- «Хочу разные диапазоны ячеек с независимыми лимитами для разных частей доски.»

## Устранение неполадок

- **Контур диапазона не виден**: убедитесь, что в диапазоне задана хотя бы одна ячейка и на доске настроены и колонки, и swimlane.
- **Бейдж показывает ноль**: проверьте, что в ячейках диапазона есть задачи и что фильтры по типам задач не исключают все задачи.
- **Отключённый диапазон всё равно показывает красный фон**: отключённые диапазоны показывают только штриховку. Если видно предупреждение WIP, проверьте, не перекрывает ли эти ячейки другой активный диапазон.
- **Настройки не сохраняются**: убедитесь, что у вас есть права на изменение конфигурации доски в Jira.

## См. также

- [Групповые лимиты WIP по колонкам](/docs/features/wip-limits/column-limits)
- [Лимиты WIP по swimlane](/docs/features/wip-limits/swimlane-limits)
- [Персональные лимиты WIP](/docs/features/wip-limits/personal-limits)
- [Общие настройки](/docs/settings)
